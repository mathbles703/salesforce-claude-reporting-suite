# Optional: only if Salesforce requires a client secret for your External Client App.
# Prefer storing the secret via Claude Code keychain:
#   MCP_CLIENT_SECRET=... claude mcp add-json ... --client-secret
# or authenticate with /mcp after .mcp.json is loaded (public client + PKCE often needs no secret).
#
# SF_CLIENT_SECRET=
